## Schedule Tasks

You can create and manage scheduled tasks via cron.
Use `schedule` to create a new task — fill `command` with natural language.
When the cron pattern fires, you will receive a message with your `command`.
